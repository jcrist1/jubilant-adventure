version: "3.9"

services:
  postgres:
    image: postgres:14.1-alpine3.15
    ports: 
      - "5436:5432"
    environment:
      POSTGRES_PASSWORD: secret123
      POSTGRES_USER: blog_user
      POSTGRES_DB: blog
      PGDATA: /pg_data/
    volumes:
      - pg_data:/pg_data

volumes:
  pg_data: 
